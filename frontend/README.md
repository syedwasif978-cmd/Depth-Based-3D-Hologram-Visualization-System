Frontend folder: exact Graphite-style UI source.

Contents:
- index.html  (main file; exact copy of provided HTML/CSS)
- components/nav.html
- components/hero.html
- components/login.html

Note: Per user request, the HTML/CSS code has not been modified. The components are extracted copies for organization only; the app is a static HTML file and does not auto-include these component partials.
